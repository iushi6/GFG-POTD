class Solution {
    public long minOperations(int N) {
        if(N%2==1) // Odd Number
        {
            return (N/2)*1L*(N/2+1);
        }
        else // Even Number
        {
            return (N*1L*N)/4;
        }
}
}
